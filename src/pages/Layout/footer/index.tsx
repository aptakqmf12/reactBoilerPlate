import React from "react";
import { StyledFooter } from "./style";

const Footer = () => {
  return (
    <StyledFooter>
      <address>footer</address>
    </StyledFooter>
  );
};

export default Footer;
