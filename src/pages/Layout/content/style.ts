import styled, { css } from 'styled-components';
import { PC, MOBILE } from '@assets/constant/number';

export const StyledWapper = styled.footer`
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
`;
